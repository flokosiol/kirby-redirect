title: Redirect
pages: true
files: false
fields:
  title:
    label: Title
    type:  text
  help:
    type: info
    help: Leave the following fields empty to redirect to the first visible child (if exists) or to the frontpage.
  redirect:
    label: Redirect to page
    type: page
    help: If set, the user will be redirected to the page (if exists)
  external:
    label: Redirect to external URL
    type:  url
    help: Will be ignored, if 'Redirect to page' is set and valid
